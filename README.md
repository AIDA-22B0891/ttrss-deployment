# Часть 2. Разработка пайплайна обработки данных (R & AI)

Реализован программный модуль на языке R для автоматизации сбора, классификации и суммаризации новостей.

## ✅ 2.1. Разработка API-клиента для TT-RSS на R

Реализован модуль [`R/ttrss_client.R`](r_pipiline/R/ttrss_client.R:1) с использованием библиотеки `httr2`, который обеспечивает авторизацию через API, получение `session_id`, фильтрацию и получение только непрочитанных (fresh) новостей, а также конвертацию JSON данных в R DataFrame.

## ✅ 2. Интеграция с YandexGPT (LLM)

Разработан модуль [`R/llm_client.R`](r_pipiline/R/llm_client.R:1) для взаимодействия с Yandex Cloud Foundation Models с настроенным системным промптом для выполнения трех задач за один проход: классификации, суммаризации и оценки релевантности, с последующим парсингом ответа нейросети из JSON в структуру списка R.

## ✅ 2.3. Модуль хранения данных

Разработан модуль [`R/db_storage.R`](r_pipiline/R/db_storage.R:1) для подключения к PostgreSQL базе данных, создания таблицы `news_analysis` при необходимости и сохранения результатов анализа новостей в базу данных.

## ✅ 2.4. Модуль очистки текста

Разработан модуль [`R/text_cleaner.R`](r_pipiline/R/text_cleaner.R:1) для удаления HTML тегов и сущностей из текста новостей, подготовки текста для корректной обработки LLM и обеспечения качества входных данных для нейросетевой обработки.

## ✅ 2.5. Сборка ETL-пайплайна

Создан управляющий скрипт [`R/pipeline.R`](r_pipiline/R/pipeline.R:1), который объединяет все компоненты системы: настроена работа с переменными окружения (.env) для безопасности ключей API, реализована интеграция всех компонентов, внедрена обработка ошибок и логирование процесса, а также проведено успешное тестирование на реальных новостях.

## ✅ 2.6. Аналитический дашборд

Разработана функция [`R/run_dashboard.R`](r_pipiline/R/run_dashboard.R:1) для запуска интерактивного дашборда с визуализацией обработанных новостей из базы данных. Дашборд включает:
- Обзор количества новостей по категориям
- Временную динамику публикаций
- Распределение оценок релевантности
- Таблицу последних новостей с возможностью фильтрации

## 📋 Полное описание пайплайна

### Общая архитектура

Пайплайн автоматически получает непрочитанные новости из TT-RSS, очищает их от HTML-разметки, анализирует с помощью YandexGPT (классификация, суммаризация, оценка релевантности), сохраняет результаты в PostgreSQL и отмечает новости как прочитанные.

### Компоненты системы

#### 1. Клиент TT-RSS ([`R/ttrss_client.R`](r_pipiline/R/ttrss_client.R:1))
- Авторизация через API и получение `session_id` для последующих запросов
- Фильтрация и получение только непрочитанных (fresh) новостей

#### 2. Клиент YandexGPT ([`R/llm_client.R`](r_pipiline/R/llm_client.R:1))
- Настроенный системный промпт для выполнения трех задач за один проход: классификации, суммаризации и оценки релевантности

#### 3. Модуль хранения данных ([`R/db_storage.R`](r_pipiline/R/db_storage.R:1))
- Подключение к PostgreSQL базе данных и создание таблицы `news_analysis` при необходимости

#### 4. Модуль очистки текста ([`R/text_cleaner.R`](r_pipiline/R/text_cleaner.R:1))
- Удаление HTML тегов и сущностей из текста новостей

#### 5. Основной пайплайн ([`R/pipeline.R`](r_pipiline/R/pipeline.R:1))
- Управление всем процессом обработки и интеграция всех компонентов системы

### Процесс обработки

1. **Подключение к сервисам**:
   - Подключение к TT-RSS и PostgreSQL, создание таблицы `news_analysis` при необходимости

2. **Получение новостей**:
   - Запрос непрочитанных новостей из TT-RSS

3. **Обработка каждой новости**:
   - Очистка текста от HTML, классификация и суммаризация с помощью YandexGPT, сохранение результатов в базу данных и отметка новости как прочитанной в TT-RSS

4. **Обработка ошибок**:
   - Обработка ошибок на каждом этапе с продолжением обработки остальных новостей и логированием

### Используемые переменные окружения

- `TTRSS_URL`, `TTRSS_USER`, `TRSS_PASS` - параметры для подключения к TT-RSS
- `YA_FOLDER_ID`, `YA_API_KEY` - параметры для доступа к YandexGPT
- `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASS` - параметры для подключения к PostgreSQL

## 🗄️ Структура базы данных

### Таблица `news_analysis`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | SERIAL | Уникальный идентификатор записи (первичный ключ) |
| `news_id` | TEXT | Уникальный идентификатор новости из TT-RSS |
| `title` | TEXT | Заголовок новости |
| `link` | TEXT | Ссылка на новость |
| `published_at` | TIMESTAMP | Дата публикации новости |
| `category` | TEXT | Категория новости, определенная с помощью ИИ |
| `summary` | TEXT | Краткое содержание новости, сгенерированное с помощью ИИ |
| `score` | INTEGER | Оценка релевантности новости, определенная с помощью ИИ |
| `created_at` | TIMESTAMP | Дата и время создания записи в базе данных (автоматически заполняется текущим временем) |

### Диаграмма структуры таблицы

```
+-------------------+-------------------+------------------+
|      Поле         |       Тип         |    Описание      |
+-------------------+-------------------+------------------+
| id                | SERIAL            | Первичный ключ   |
| news_id           | TEXT (UNIQUE)     | ID новости TT-RSS|
| title             | TEXT              | Заголовок        |
| link              | TEXT              | Ссылка на новость|
| published_at      | TIMESTAMP         | Дата публикации  |
| category          | TEXT              | Категория (ИИ)   |
| summary           | TEXT              | Суммаризация (ИИ)|
| score             | INTEGER           | Оценка (ИИ)      |
| created_at        | TIMESTAMP         | Дата создания    |
+-------------------+-------------------+------------------+
```
